# name repo_url (cloned into $ZSH_CUSTOM/themes/<name>)
# The active theme (fishy) is built into oh-my-zsh, so no external theme is
# listed by default. Add a line here only for themes that need cloning.
